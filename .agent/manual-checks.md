# manual-checks.md — 手動確認チェックリスト

> セルフテスト（`TAKO_SELF_TEST=1`）で機械検証できない、人間の目と実際の OS 統合が
> 必要な確認項目。リリース前・該当機能の変更時に通す。

## プレビュー画像メモリ上限（Issue #258）

LRU 会計、CLI / MCP、RSS 長時間系列は機械検証する。実 Metal texture 解放後の見た目と
スクロール操作感は macOS 実機で以下を確認する。

- [ ] 50ページ以上の PDF を開き、先頭から末尾までスクロールしても現在ページの前後が
      白いまま残らず、読み込み待ち後に鮮明な画像へ切り替わる
- [ ] 400% までズーム後にページ移動・100% 復帰を繰り返しても、表示中ページが LRU の
      近傍ページより先に消えず、パン・文字選択・目次ジャンプが従来どおり効く
- [ ] プレビューを BG 退避・復帰、close しても別ペインの同一画像が壊れず、GPU の残像や
      一瞬の黒塗りが出ない
- [ ] `tako preview-cache 256` で上限縮小後も操作感が劣化せず、再起動後に設定が維持される

## IME 変換中表示（FR-1.9、Phase 3.5）

セルフテスト 37〜39 は EntityInputHandler の状態遷移（保持・確定・unmark）のみを検証する。
実際の IME（NSTextInputClient）経由の見た目・候補ウィンドウは以下を手動確認する。

前提: macOS の日本語入力（ライブ変換 OFF 推奨で確認が安定）。`cargo run -p tako-app` で起動。

- [ ] 日本語 IME で「nihongo」と打つ → カーソル位置に「にほんご」が**下線付き**で
      インライン表示される（確定前。シェルには何も送られていない）
- [ ] スペースで変換 → 変換候補に置き換わり、**注目文節が太い下線 + 選択色**で強調される
- [ ] 変換候補ウィンドウが**変換中テキストのすぐ下**に出る（画面左上や別ペインに出ない）
- [ ] enter で確定 → 確定文字列が**1 回だけ**シェルへ入る（二重入力しない）
- [ ] esc でキャンセル → 未確定文字列が消え、シェルには何も送られない
- [ ] 複文節（例:「きょうはいいてんき」）でカーソルキーで文節を移動 → 太下線が文節に追従する
- [ ] 変換中にウィンドウをリサイズしても表示位置がカーソルに追従する
- [ ] ペイン分割後、**フォーカス中のペイン**のカーソル位置に変換中テキストが出る
- [ ] 英数モードでの直打ち（ASCII）が二重入力しない（stop_propagation の回帰確認）
- [ ] dead key（ABC 配列で option-e → e = é など）が機能する
- [ ] cmd-d / cmd-t 等のショートカットが IME 有効時（ひらがなモード）でも効く

既知の制約（実装メモ。`crates/tako-app/src/main.rs` の EntityInputHandler 実装コメント参照）:

- 変換中テキストはペイン右端で折り返さない（長い未確定文字列はペイン外へはみ出す）。
  日常入力では文節確定が先に来るため許容。問題になれば折り返し対応を検討
- スクロールバック表示中（カーソル非表示）は変換中テキストのアンカーが取れないため
  オーバーレイ・候補ウィンドウの位置出しを行わない（変換自体は可能で確定は届く）

## ペイン境界ドラッグリサイズ（Phase 1 残骨格）

座標→比率換算とツリー更新はセルフテスト 5b で機械検証済み。カーソル形状と手触りは目視で確認:

- [ ] cmd-d で左右分割 → 境界線にマウスを近づけると**左右リサイズカーソル**（⇿）になる
- [ ] cmd-shift-d で上下分割 → 境界線で**上下リサイズカーソル**（↕）になる
- [ ] 境界をドラッグ → ペイン比率がマウスに追従して滑らかに変わり、PTY も即リフロー（cols/rows 追従）
- [ ] ネストした分割（例: 右ペインをさらに上下分割）で**内側の境界だけ**が動く（外側は無傷）
- [ ] 端まで引いても潰れない（MIN_SHARE/MAX_SHARE=0.1/0.9 でクランプ）
- [ ] 境界ハンドル上でのドラッグは選択（copy-on-select）を誤発火しない
- [ ] ドラッグ中にウィンドウ外でマウスを離した後、ペイン内の通常クリック・ドラッグで
      リサイズが誤発火しない（カーソル形状が変わる領域とドラッグ開始領域が常に一致する。
      機械検証はセルフテスト 5c）

## シェル統合（FR-2.4.1、Phase 4）

zsh の自動注入（ZDOTDIR 方式）はセルフテスト 41/41b で機械検証済み。
bash / fish は注入経路（PROMPT_COMMAND / XDG_DATA_DIRS）が異なるため手動確認する:

- [ ] ペイン内で `bash` を起動 → `cd /tmp` → `tako list` の該当ペインに cwd が出る
- [ ] 同 bash で `false` → `tako list` の state が failed / exit_code 1
- [ ] ペイン内で `fish` を起動（インストール済みなら）→ 同上
- [ ] p10k / starship 等のプロンプトカスタマイズと共存できる（プロンプト崩れなし）
- [ ] `TAKO_NO_SHELL_INTEGRATION=1 tako-app` で統合が無効化される（cwd / state が unknown のまま）

状態インジケータ（FR-2.1.4。集約ロジックはユニットテスト済み、見た目を目視）:

- [ ] `sleep 5` 実行中、ペイン右上にアクセント色ドット + タブにも同色ドットが出る
- [ ] `false` 実行後、赤ドットに変わり、次のコマンド実行開始まで保持される
- [ ] 非アクティブタブのペインが失敗しても、そのタブに赤ドットが出る（タブ横断の把握）
- [ ] プロンプトに戻って正常終了（exit 0）したらドットが消える

## tmuxview タブ（FR-2.13）

コマンド層・状態遷移はセルフテスト 48/49 で機械検証済み。見た目と操作感を目視で確認:

- [ ] タブバー右端に「tmux」タブが固定表示され、× が無い（閉じられない）
- [ ] tmux セッションがあるとき、名前 / attached・detached / 作成時刻 / window 一覧が出る
- [ ] tako 内のペインで `tmux attach` したセッションに「tako タブ N / ペイン M」が出る
- [ ] 外部ターミナル（iTerm2 等）で attach したセッションは「tako 外」と出る
- [ ] kill ボタン → インライン確認 →「kill する」で消える、「やめる」で何も起きない
- [ ] 表示中に外部で tmux を作る / 殺すと 2 秒以内に一覧へ反映される
- [ ] 通常タブのクリック・cmd+数字・cmd+T で tmuxview から抜けられる

## role / title バッジ表示（FR-2.1.3、Phase 3 残）

role / title の設定・取得自体はセルフテスト（`tako title / role 設定`）で機械検証済み。
バッジの見た目は目視で確認:

- [ ] `tako title --role worker-1 REVIEWER` → ペイン右上に「REVIEWER · worker-1」バッジが出る
- [ ] role のみ / title のみ設定でもバッジが出る（両方未設定なら出ない）
- [ ] バッジがターミナル 1 行目の文字と重なっても読める（タブバー背景色の下地）
- [ ] バッジ上のクリックでもペインフォーカス・選択が通常どおり動く

## .app バンドル（Phase 3.5）

`scripts/build-app.sh --verify` の機械検証（バンドル版バイナリでのセルフテスト 39 項目）に
加えて以下を手動確認する。

- [ ] `dist/tako.app` をダブルクリック（または `open dist/tako.app`）で起動する
- [ ] Dock にタコのアイコン（icon-a）が表示される
- [ ] Dock から起動した tako 内で `tako list` が通る（Finder 起動でも TAKO_* 注入が生きる）
- [ ] Dock から起動した tako 内で Claude Code が MCP 接続できる
      （初回登録済みなら設定ゼロ。`claude mcp list` で tako が ✓）
- [ ] /Applications へコピーして同上が通る

## AI 自動リネーム（FR-2.12）

トグル・rename API・手動優先・ヒューリスティック適用はセルフテスト（項目 50〜52）で
機械検証済み。claude CLI を実際に呼ぶ経路と見た目は常用で確認:

- [ ] コマンド実行が一段落して数秒後（静穏 4 秒 + クールダウン 30 秒）、タブ名・ペイン名が
      作業内容に沿った短い日本語名へ変わる
- [ ] `tako title` / `tako tab rename` で手動命名したタブ・ペインは自動で上書きされない
- [ ] `tako tab rename --tab N ''`（空文字）で手動指定が解除され、自動リネームが再開する
- [ ] `tako autorename off` で止まり、再起動後も OFF が維持される（settings.json）
- [ ] claude CLI の無い環境（PATH から外す）で OSC タイトル / cwd 由来の名前に劣化する

## listen ポート検知と提案チップ（FR-2.4.2〜2.4.4）

検知・チップの状態遷移・OFF トグルはセルフテスト（項目 53〜55）で機械検証済み。
見た目と実サーバーでの体験は常用で確認:

- [ ] `npm run dev` 等で dev サーバーを起動すると数秒以内にペイン下端へ
      「localhost:PORT（プロセス名）が listen 中」チップが出る
- [ ] 「ブラウザで開く」で既定ブラウザに該当 URL が開き、チップが消える
- [ ] 「×」で消え、同じサーバーが動いている間は再提案されない（再起動で再提案）
- [ ] `tako portdetect off` でチップ・list の listen_ports が消え、再起動後も OFF が維持される

## 集約センター（FR-2.10）

一覧の収集・並び・ジャンプはセルフテスト（項目 56）で機械検証済み。見た目は常用で確認:

- [ ] タブバー右端の「agents」タブで全タブのペイン一覧が出る（エラー > 入力待ち > 実行中の順）
- [ ] どこかのペインがエラー / 実行中のとき agents タブにドットが出る
- [ ] エントリのクリックで該当ペインへ飛ぶ（別タブならタブも切り替わる）
- [ ] エージェント（Claude Code 等）が入力待ちになったとき一覧の上位に来る

## ファイルツリー（FR-3.1 / FR-3.7）

開閉・cwd 追従・展開・更新検知はセルフテスト（項目 57）+ ユニットテストで機械検証済み。
見た目は常用で確認:

- [ ] cmd+B で左サイドバーが開閉し、閉じれば純粋なターミナルに戻る
- [ ] `cd` するとツリーの root が数秒以内に追従する
- [ ] ディレクトリ行クリックで展開（▸/▾）、巨大ディレクトリでも固まらない
- [ ] ファイル行クリックは現状なにもしない（FR-3.2 のプレビュー実装待ち）

## tmux バックエンド永続化（FR-5、Phase 5.5）

セッション生成・OSC パススルー・tty 差し替え・close 時 kill・detach → 再 attach 復元は
セルフテスト（項目 58〜62）+ core e2e テストで機械検証済み。実 .app での体験を常用で確認:

- [ ] **再起動復元**: ペイン分割 + タブ数枚 + 実行中プロセス（`sleep 999` や dev サーバー）を
      作り、tako を cmd+Q → 再起動。タブ構成・分割・実行中プロセス・画面内容が戻る
- [ ] **AI 操作の継続**: 再起動前から動いている claude（tmux セッション内で生存）が、
      再起動後も MCP ツール（`tako_list_panes` 等）を叩ける（control.json フォールバック）
- [ ] **PC 再起動相当の Claude 会話復旧（#139）**: Claude を起動して layout.json に
      `claude_session_id` が保存されたことを確認 → tako 終了 + 専用 tmux server kill →
      tako 再起動。同じペインで `claude --resume` が起動し、直前の会話が表示される。
      tmux server を残す通常再起動では既存 Claude への再 attach だけで二重起動しない。
      `tako persist` / MCP `tako_persist` の `last_restore` に resume 件数が出る
- [ ] 体感レイテンシ・描画が直接 spawn と変わらない（キー入力・大量出力・vim / claude の TUI）
- [ ] ホイールスクロールで過去出力が見える（tmux copy-mode。`q` か最下部までスクロールで戻る。
      従来の自前スクロールバック・スクロールバーは backend ペインでは出ない = 既知の仕様変更）
- [ ] Shift+Enter（claude の改行）・IME 日本語入力が tmux 越しでも効く
- [ ] ペイン内で素の `tmux` / `tmux attach` が今まで通り使える（ネストにならず既定サーバーへ。
      TMUX unset の効果）。その attach ペインを × で閉じても外部セッションは死なない
- [ ] tmuxview に「tako 永続化」バッジ付きでバックエンドセッションが見え、対応ペインが出る。
      kill 確認の文言が専用のもの（中身が消える旨）になっている
- [ ] `tako persist off` → 新規ペインは直接 spawn に戻る（既存ペインはそのまま）。
      `on` に戻すと新規ペインが再び永続化される
- [ ] 異常系: `tmux -L tako kill-server` で全セッションを消してから tako を起動しても、
      レイアウトだけ復元され各ペインは保存 cwd の新しいシェルで開く（落ちない）
- [ ] tmux 未導入環境相当（`TAKO_TMUX_BIN=/nonexistent tako-app` で起動）でもタブ構成が
      保存され、再起動で復元される（各ペインは保存 cwd の新シェル。Issue #30）。
      `tako persist` が available=false + last_restore に「tmux 不在」の復元結果を返し、
      `<data_dir>/persist.log` に復元成否が記録されている

## 実機リグレッション修正一括（2026-06-12 夕）

tmux バイナリ解決・マウス / CSI u の生配送・明示コマンド split・IME 位置は機械検証済み
（core e2e + セルフテスト 63〜64）。実 .app で以下を確認:

- [ ] **Dock 起動の .app で** tmuxview（パネル tmux ビュー）に既定サーバーのセッションが出る
      + `tako persist` が `"available":true`（tmux_bin のログインシェル解決）
- [ ] バックエンドペインの claude 内でホイール → チャットが遡れる（copy-mode）。
      入力履歴が暴発しない（矢印キー化の根絶）
- [ ] バックエンドペインの claude で Shift+Enter 改行が効く（CSI u 強制 + csi-u 形式）。
      **tmux 未導入環境（直接 spawn）でも同様に効く**（Issue #28 で全ペイン既定化。
      GUI 実キー経路はセルフテスト 45b、実 claude e2e は `TAKO_SELF_TEST_CLAUDE=1` の
      45c で機械検証可。残るは Homebrew 配布ビルド実機での確認のみ）
- [ ] 日本語の長文入力で IME 候補ウィンドウがカーソル直下に出続ける（全角行の shaping 位置）
- [ ] `tako split --pane N --down -- tmux attach -t xxx` が通る（ログインシェル経由実行）
- [ ] 右サイドバーパネル: ~~タブバー右端の「◧ panel」で開閉~~ → 下部ステータスバーの
      トグルで開閉（FR-2.16.4 で刷新。後述「パネル UI 刷新」節）、左端ドラッグで幅調整、
      内部タブ tmux ⇔ git 切替、`tako panel --show --view tmux` でも操作できる
- [ ] ペインタイトルバー: × で閉じる・状態ドット・リネーム名が出る。タイトルバーの
      クリックでフォーカス移動（選択は始まらない）

## ウィンドウジオメトリの復元（FR-5 追補、2026-06-12）

保存・読み込みの往復はユニットテストで機械検証済み。実 .app で確認:

- [ ] ウィンドウをリサイズ・移動して再起動 → 同じサイズ・位置で開く
- [ ] フルスクリーンのまま再起動 → フルスクリーンで開く（解除すると保存時の復元サイズに戻る）
- [ ] layout.json が無い初回起動は従来どおり中央 960×600 で開く

## スクロール・キー実機バグ一括（2026-06-12 夜）

バグ (2) のインジケータ非表示と conf 再適用は core e2e で機械検証済み。実 .app で確認:

- [ ] 通常シェルペインでホイールスクロール中、右上に時刻・`[n/m]` 表示が**出ない**
      （tako 再起動後 = sync_conf 適用後。出る場合は稼働サーバーへの conf 再適用を疑う）
- [ ] ユーザー自前 tmux（master-* 等）を attach したペイン内の claude でホイール →
      チャットが遡れる（~/.tmux.conf の mouse on 経由。ネスト tmux の copy-mode）
- [ ] 同ペインの claude（**設定適用後に起動し直したもの**）で Shift+Enter 改行が効く
      （~/.tmux.conf の extended-keys always。適用前から起動している claude には効かない）

スクロール制御の方式転換（tako 駆動 copy-mode + コアレッシング）後の確認:

- [ ] claude のチャットをホイールで遡るとき、5 行単位で「ばっ」と飛ばず
      回した量に比例して滑らかに動く（トラックパッドのゆっくりスクロール含む）
- [ ] スクロールで遡った状態のままキーを打つと、最下部へ戻ってから入力が
      claude に届く（入力が消えない。iTerm2 と同じ）
- [ ] スクロール中、白いブロックカーソルが画面に居座らない（最下部復帰で戻る）
- [ ] スクロールするとペイン右端にスクロールバーが現れ、止めると約 1.4 秒で
      フェードアウトする。バーのドラッグで位置移動できる
- [ ] `tako scroll --delta 20` / `--to 0` がバックエンド・ネスト両ペインで効き、
      実行時にスクロールバーも表示される

## パネル UI 刷新（FR-2.16.4〜2.16.8、2026-06-12）

タブ枠データ・kill 経路・CLI roundtrip はセルフテストで機械検証済み。実 .app で確認:

- [ ] 下部ステータスバー: 左「◫ ファイル」でファイルツリー開閉（cmd+B と同じ）、
      右「⌗ tmux」「⎇ git」で情報パネルの該当ビューが開閉する。タブバー右端に
      旧「◧ panel」ボタンが**無い**
- [ ] 統合 tmux ビュー: タブごとの枠に全ペインが入れ子で出る。行クリックでジャンプ、
      ゴミ箱 → 「kill していいですか?」→ kill でペインが消える。長い名前・cwd が
      省略（…）され見切れない
- [ ] タブ未表示セクション: ユーザー直起動の tmux が「管理外」、orphan バックエンドが
      「kill漏れ?」ラベルで出て、それぞれ確認つきで kill できる
- [ ] `tako panel --filetree on/off` / `--view git` が UI に反映される
- [ ] git ビューはプレースホルダ文言が出る（git graph は FR-3.6）

## Esc「27u」挿入バグ修正（2026-06-12）

素の `\e` 往復・「27u」非漏出・CSI u 維持は core e2e + 単体テストで機械検証済み。
実 .app で確認:

- [ ] 素の zsh ペインで Esc を押しても「27u」が入力欄に**挿入されない**
- [ ] claude ペインで Esc が効く（入力キャンセル等）+ Shift+Enter 改行が引き続き効く
- [ ] ネスト tmux（自前サーバー attach）内の claude でも Esc / Shift+Enter が効く
- [ ] vim ペインで Esc によるモード切替が遅延なく効く（escape-time 10ms の範囲）

## 実機バグ 3 件一括修正（FR-2.16.9 ほか、2026-06-13）

attach 済みセッションの紐付け・管理外からの除外・kill 経路はセルフテスト 61f で
機械検証済み。実 .app で確認:

- [ ] 別サーバーの tmux セッション（例: orchestrator の master-tako）を tako ペインで
      `tmux attach` していると、パネルの tmux ビューで**そのタブの枠内**に
      「tmux セッション名 + ペイン N で attach 中 + window 一覧（worker 等）」が出る。
      「管理外」セクションには**出ない**
- [ ] attach をやめる（detach / 終了）と、そのセッションは「管理外」へ戻る
- [ ] kill 確認 UI: 長い確認文言でも折り返され、「kill する / やめる」ボタンが
      パネル右端に見切れない（ペイン kill・セッション kill・window kill すべて）
- [ ] 下部ステータスバーが消えない: ファイルツリーで大きなディレクトリを展開
      した状態 + tmux ビューにセッションが大量にある状態 + ウィンドウを小さく
      リサイズ、のどの組み合わせでもバーが表示されたまま

## ワークスペース機能第 1 弾（FR-3.1 改 / FR-3.2 / FR-3.3、2026-06-13）

OpenFile の dispatch / CLI / MCP 経路・再利用・モード切替・マルチルート同期は
セルフテスト 66〜67 で機械検証済み。見た目と操作感を実 .app で確認:

- [ ] ファイルツリーのファイル行クリック → 右半分にプレビューペインが開き、
      コードに**シンタックスハイライト + 行番号**が付く（Rust / TS / JSON 等）
- [ ] 別ファイルをクリック → 新しいペインが増えず**同じプレビューペインが差し替わる**
- [ ] `.md` ファイルは**レンダリング表示**（見出しサイズ・リスト・コードブロックの
      ハイライト・引用・罫線）で開き、タイトルバー右の「</> コードとして表示」⇔
      「👁 md レンダリング表示」トグルがワンクリックで効く
- [ ] プレビューのスクロール（ホイール）が滑らかで、巨大ファイルは
      「…（大きいファイルのため末尾を省略して表示）」が出て固まらない
- [ ] バイナリファイル（画像等）はエラーメッセージ表示に劣化する（画像表示は FR-3.10）
- [ ] ファイルツリー: タブ内でペインごとに cwd が違うと、**ルート見出し（🗂 太字）が
      複数並ぶ**。`cd` すると 2 秒以内に追従し、ペインを閉じるとそのフォルダが消える
- [ ] プレビュー表示中のファイル行がツリー上でアクセント色になる
- [ ] `tako open README.md` / claude から「README をプレビューで開いて」
      （`tako_open_file`）が UI クリックと同じ挙動になる
- [ ] persist ON で再起動 → プレビューペインが同じファイル・同じモードで戻る
- [ ] 長いコード行は右端で切れる（横スクロールは未対応 = 既知の制約。
      必要になったら FR-3.5 の編集系と合わせて検討）

## プレビューライブリロード（FR-3.15、2026-07-15、#233）

OS イベント、連続 write のデバウンス、削除 / rename / 巨大ファイル、
画像 / PDF、編集競合は隔離セルフテスト 66c で機械検証する。実 .app では体感と
他エディタの原子保存経路を確認:

- [ ] Markdown 表示中に外部エディタで数回連続保存すると、最終内容が
      おおむね 300ms〜1 秒で表示され、中間内容の点滅がない
- [ ] 更新前後で縦スクロール位置と code / Markdown モードが変わらない
- [ ] 画像 / PDF 更新後も zoom / pan が変わらず、PDF は更新内容が鮮明に再描画される
- [ ] 編集モード中に外部保存すると競合通知が出て、編集中バッファは変わらない
- [ ] `tako preview-reload off` 中は更新されず、`on` へ戻した後の次の保存から再開する
- [ ] 動画プレビューは外部保存で再読み込みされず、再生位置を保持する（仕様）

## プレビュー目次ナビゲーション（FR-3.16、2026-07-15、#232）

重複 / 0 件の Markdown 見出し、PDFKit アウトラインあり / なし、ページ指定、
ライブリロード後の目次差し替えは隔離セルフテスト 66 〜 66c で機械検証する。
実 .app ではドロップダウンの見た目とクリック後の位置を確認:

- [ ] H1〜H6 を含む長い Markdown でヘッダの「目次」をクリックすると、階層インデント付きの
      アウトラインが開き、各行クリックで対応見出しが表示領域の先頭へ移動する
- [ ] 同名見出しが複数ある場合もそれぞれの位置へ移動し、見出し 0 件では「目次」ボタンが出ない
- [ ] PDFKit アウトライン付き PDF で「目次」を開くと項目が並び、クリックで対応ページへ移動する。
      アウトラインなし PDF では「目次」ボタンだけが出ず、PDF 本文は従来どおり表示される
- [ ] PDF ヘッダの `n / 全ページ数` をクリックするとページ一覧が開き、任意ページをクリックすると
      そのページが先頭へ移動してヘッダの `n` も更新される
- [ ] Markdown を外部保存して見出しを増減すると、本文と目次が同時に更新され、開いていた
      スクロール位置を不必要にリセットしない
- [ ] `tako preview-outline --pane N` / `--item N` と MCP `tako_preview_outline` が GUI と同じ
      一覧・ジャンプになり、`tako preview --pane N --page N` / MCP `tako_preview_view` も同じ
      PDF ページジャンプになる
- [ ] 目次パネルを閉じたアイドル状態で `perf_span("render")` が従来の 0ms 水準を維持する

## コードプレビュー軽量編集（FR-3.5、2026-07-12、#126）

core / dispatch / CLI / MCP の編集・保存、日本語、空 / 5000 行 / 読み取り専用 / 外部変更は
unit test とセルフテスト 66c で機械検証する。実 .app では GPUI / macOS IME の操作感を確認:

- [ ] コードプレビューの「✎ 編集」→「✓ 編集中」が切り替わり、クリック位置へキャレットが出る
- [ ] ASCII と日本語を入力し、BS / Delete / Enter / 矢印 / Home / End が文字化けや panic なく動く
- [ ] マウスドラッグと Shift+矢印の選択後、入力・貼り付けで選択範囲が置換される
- [ ] 未保存時にファイル名の「●」と「保存 ⌘S」が表示され、⌘S 後に消える
- [ ] 保存後に外部ターミナルの `cat` で内容が一致する。外部で先に変更した場合は警告され、
      外部内容を上書きせず dirty が残る
- [ ] PDF / 画像 / 動画には編集ボタンが出ず、PDF #124 のドラッグ選択・⌘C が従来どおり動く
- [ ] 1MB または 5000 行を超えて省略表示されたファイルは編集不可になり、末尾を失わない

## プレビュー選択座標・編集時色分け（2026-07-13、#145）

座標→UTF-8 byte index の往復はセルフテスト 66b-2、PDF の変換・コピーは unit / セルフテストで
機械検証する。実マウスのドラッグとハイライト合成結果は実 .app で確認:

- [ ] コードプレビューの行頭→行末をドラッグし、カーソル直下の文字だけが選択される
- [ ] 日本語と半角が混在する行・タブを含む行でも、ドラッグ位置と選択端が一致する
- [ ] 長いコードを縦スクロールした後も、表示中の行を正しい位置で選択できる
- [ ] Markdown の H1 / 本文 / リスト / コードブロックで、文字サイズや字下げに関係なく選択位置が一致する
- [ ] PDF を通常表示・ウィンドウ幅変更後の各状態でドラッグ選択し、半透明ハイライトが文字上に出る
- [ ] PDF の選択を ⌘C し、貼り付け先で同じ文字列（日本語を含む）が得られる
- [ ] 編集モードでも読み取りモードと同等の構文色が付き、選択ハイライトとキャレットが同時に見える

## PDF 選択描画・標準言語セット色分け（2026-07-13、#152）

`TAKO_DISCOVERY_DIR=<一時dir> TAKO_PERSIST=0 TAKO_VISUAL_TEST=1 cargo run -p tako-app --features visual-test`
は Metal の最終 scene を直接 RGBA へ読み戻し、PDF 選択前後と C++ / Python の平文→読み取り色→
編集色の対象矩形ピクセル差分を機械検証する。実 .app では次を最終確認:

- [ ] PDF の文字列を実マウスでドラッグし、青い半透明ハイライトが文字の真上へ出る
- [ ] PDF を縦スクロール・ウィンドウリサイズした後も、選択位置とハイライトが追従する
- [ ] PDF の行間またはページ余白からドラッグしても、全文選択にならず選択が開始しない
- [ ] Retina 画面で PDF を開き、`TAKO_PDF_RASTER` の pixels が表示幅 × device scale 以上に
      なり、固定 2x 画像の拡大表示より文字・斜線がシャープに見える
- [ ] PDF と画像でピンチ・⌘+/⌘-・ヘッダの ± が効き、ペイン寸法を変えず倍率表示が更新される
- [ ] 150% 以上で 2 本指スクロールすると上下左右へパンでき、⌘0 / 倍率クリックで 100% と
      左上位置へ戻る。通常の縦スクロールとピンチ中心の位置も不自然に跳ばない
- [ ] PDF を 150% 以上へ拡大・パンした後に文字をドラッグし、選択端と半透明ハイライトが
      文字の真上へ一致する。`tako preview --zoom 150 --page 1` と MCP
      `tako_preview_view` でも同じ倍率・ページ・パン状態を操作・取得できる
- [ ] `.cpp` / `.py` を読み取り表示と編集表示で開き、キーワード・文字列・数値等が複数色になる
- [ ] `.rs` / `.js` / `.ts` / `.sh` も同様に複数色になり、編集・保存後も色が維持される

## ターミナルリンクの cmd+クリック・cmd ホバー装飾（2026-07-13、#153）

検出・解決・クリック→アクション（ファイル = 分割プレビュー / ディレクトリ = 分割 + PTY 起動）は
セルフテスト 69c で機械検証する。装飾の見た目と実マウス操作は実 .app で確認:

- [ ] cmd を押しながらパス / URL にホバーすると、リンク文字列だけに下線 + アクセント色 + 背景ハイライトが出る（行全体に広がらない）
- [ ] マウスを動かさず cmd を押す・離すだけでも装飾が即時に出る・消える
- [ ] cmd ホバー中のカーソルがポインタ（人差し指）になる
- [ ] claude TUI（master ペイン、cwd は `~`）で絶対パス / `~/` 起点 / cwd 相対パスが cmd+クリックで開ける
- [ ] ディレクトリの cmd+クリックで右分割ペインが開き、シェルがそのディレクトリで起動する（空ペインにならない）
- [ ] URL の cmd+クリックは従来どおりブラウザで開く
- [ ] 回帰: 通常のドラッグ選択がペインのテキスト領域を外れても伸び続ける / スクロール・コピペに変化がない

## ターミナルスクロールの大幅改善（2026-07-13、#159）

サブライン描画（ピクセル単位）の正しさは visual-test（`TAKO_VISUAL_TEST=1 --features
visual-test` の「半行戻しでほぼ一致」）で、状態遷移・ミラー・CLI 経路はセルフテスト
44b / 61b〜61e で機械検証する。**操作感そのもの**は実 .app + 実デバイスで確認:

- [ ] トラックパッド 2 本指スクロールが**ピクセル単位で滑らか**に動く（1 行ごとの
      カクカクした段付きにならない。直接ペイン / tmux バックエンドペイン両方）
- [ ] 指を離した後の**慣性（モメンタム）**が自然に減速して止まる（Chrome / iTerm2 と
      並べて比較して違和感がない）
- [ ] ゆっくりの微小スクロールでも反応する（旧: 1 行分溜まるまで無反応）
- [ ] マウスホイールは 1 ノッチ = 3 行で従来どおりの感覚
- [ ] スクロール中に右端へオーバーレイスクロールバーが出て、停止 約 1.4 秒で
      フェードアウトする
- [ ] スクロールバーにホバーすると表示が維持され、サムが太く・濃くなる。
      ドラッグで表示位置が追従する（つまみの位置とコンテンツが一致）
- [ ] tmux バックエンドペイン（persist ON 既定）で深く遡る → さらに上へスクロールし
      続けると追加の履歴が読み込まれて続きが見える（500 行チャンク）
- [ ] バックエンドペインで過去を見ている最中に新規出力が来ても表示位置が
      大きく飛ばない（1 秒以内の増分追従で内容固定）
- [ ] スクロール中にキー入力すると最下部へ戻って入力が効く（キーが飲まれない）
- [ ] claude / vim 等のマウス要求 TUI ではホイールが従来どおりアプリ側スクロールに
      なる（チャット履歴の遡り等）
- [ ] alt screen（less / vim）⇔ 通常画面の切替でスクロール位置がおかしくならない
- [ ] 回帰: ドラッグ選択・copy-on-select / cmd+クリックリンク（#153）/ IME 変換位置 /
      プレビューペインのスクロールに変化がない
- [ ] `tako tmux open` で取り込んだ再アタッチ / tako-view ラッパーペインでも
      ピクセルスクロール・スクロールバー・追加履歴ロードが機能する（#181。
      #177 復旧直後のような「全ペインが再アタッチ」環境が主対象）
- 既知制約（仕様）: バックエンドペインで**過去を表示中**は選択・cmd+クリックが
  効かない（最下部に戻れば可）。ミラー表示中の新規出力はライブ領域のみ動く
- 既知制約（仕様）: **alt screen TUI（claude Code / vim / less 等）の内部スクロールの
  粒度はアプリ実装依存**で、ピクセル単位スムーススクロールの対象外（描画権が TUI 側に
  あり、ホイールは SGR / 矢印キーとして転送されるだけでターミナル側は補間できない。#181）

## ドラッグ&ドロップ 3 件（FR-2.16.10 / FR-3.11 / FR-1.10、2026-06-13）

同等操作（dispatch TmuxOpen / OpenFile direction / MovePane target）と attach 実接続は
セルフテスト 68〜68c で機械検証済み。ドラッグの操作感・挿入プレビューの見た目を実 .app で確認:

- [ ] パネルの tmux ビューでセッション行（管理外 / kill漏れ? / attach 済み）を
      ドラッグすると、カーソルに**ゴーストチップ（tmux: 名前）**が追従する
- [ ] ペイン上にドラッグすると**ドロップ先がハイライト**され、カーソルの象限
      （上下左右）に応じて**新ペインが占める半面**がアクセント色 + 枠線で光り、
      「ここに分割して表示」ラベルが出る
- [ ] ドロップでその位置に分割ペインが生え、中で対象セッションが表示される
      （多重 attach 警告なし・サイズ崩れなし）。ペインを閉じてもセッションは残る
- [ ] ファイルツリーのファイル行をドラッグ → 象限で「ここに分割して開く」、
      **中央 40% で「ここで開く」**（= 既存プレビューの差し替え / 再利用）に変わる
- [ ] ドロップでプレビューペインがその位置に開く（.md はレンダリング表示）
- [ ] ペインのタイトルバー（ターミナル / プレビュー両方）をドラッグ →
      「この位置に移動」プレビューが出て、ドロップで iTerm2 同様に配置が変わる。
      自分自身の上ではプレビューが出ず、ドロップしても何も起きない
- [ ] どこにも落とさず離す / ドラッグ中に Esc → ハイライトが消えて何も起きない
- [ ] ドラッグ開始のしきい値が自然（クリック（ジャンプ・展開・フォーカス）が
      誤ってドラッグ扱いにならない。× ボタン・ゴミ箱クリックも従来どおり）
- [ ] `tako tmux open <session> --socket <S> --down` / `tako open a.rs --down` /
      `tako tab move-pane --target N --down` が D&D と同じ結果になる

## TCC 許可の保持（署名 DR 固定、Issue #54、2026-07-03）

designated requirement の identifier 固定・署名検証は build-app.sh 内で機械検証済み
（`codesign --verify -R` がビルドごとに走る）。TCC 許可が実際に保持されることは
実機でのみ確認可能。**`tccutil reset` はユーザーの付与済み許可を消すため、
必ずユーザー本人の同意を得てから実行する**:

- [ ] （準備）`tccutil reset SystemPolicyAppData dev.takushio.tako` で tako の
      「ほかのアプリからのデータ」許可をリセット（他アプリの許可には触れない）
- [ ] tako を起動し、claude worker を spawn → iCloud Drive / Google Drive 配下を
      触らせると「ほかのアプリからのデータへのアクセス」ダイアログが**対象ごとに 1 回**出る
- [ ] 許可後、同じ対象への再アクセスではダイアログが出ない（ペイン追加・spawn・
      tako 再起動でも再要求なし）
- [ ] `scripts/build-app.sh` で再ビルド → dist/tako.app と /Applications/tako.app の
      `codesign -d -r -` の designated が同一（`identifier "dev.takushio.tako"`）
- [ ] 再ビルド版へ差し替え（またはアプリ内更新）後も、付与済みの許可が保持され
      再要求されない
- [ ] （ゼロ回化の確認）システム設定 → プライバシーとセキュリティ →
      フルディスクアクセスで tako を ON にすると、以後このダイアログ自体が出ない

## Web ビューペイン（FR-3.8 / #155、2026-07-13）

セルフテスト 71 は dispatch 経由の open / list / read（タイトル追跡）/ navigate /
eval / hide / show / close を機械検証する。実マウス・キーボード・IME の操作感と
見た目は以下を手動確認する。

前提: `tako web open https://example.com` またはステータスバー確認用に複数ページを開く。

- [ ] Web ビューペイン内でリンクをクリック → 普通に遷移する（座標ずれが無い）
- [ ] ページ内スクロール（トラックパッド 2 本指）が滑らかに効く
- [ ] テキスト入力欄への文字入力・日本語 IME 変換がページ内で普通にできる
- [ ] タイトルバーの ← / → / ⟳ で戻る・進む・再読み込みが効く
- [ ] ペイン分割・リサイズ・ウィンドウリサイズに webview の位置とサイズが追従する
      （ずれ・はみ出し・チラつきが無い）
- [ ] タブ切替で webview が隠れ、戻ると再表示される（別タブに残像が出ない）
- [ ] ー ボタン → ペインが閉じ、ステータスバー 🌐 の件数が維持される。
      🌐 → dock パネル → 行クリック（または「表示」相当）で復帰し、
      **スクロール位置・入力途中の状態が残っている**（ページが生きている証拠）
- [ ] × ボタン → ページごと破棄され、dock 一覧からも消える
- [ ] tako 再起動 → 表示中だった webview ペインが同じ URL で開き直される。
      dock 退避分も 🌐 一覧に復元される（ページ内状態はリセットで仕様どおり）
- [ ] ターミナルペインへフォーカスを移す → キー入力が webview に吸われず
      ターミナルに入る（cmd+T 等のショートカットも効く）

既知の制約（`architecture.md`「Web ビューペイン」節）:

- ネイティブビューは GPUI 描画の上に乗るため、**ピン留め窓・ホバープレビュー・
  コンテキストメニューは webview ペインの上では隠れる**（ドロワー・サイドバー・
  dock パネルは flex レイアウト内のため重ならない）
- D&D 中は全 webview が一時的に隠れる（ドロップターゲットを見せるため。仕様）
- webview の中身はスクリーンサムネイル系（たまり場カード・ホバープレビュー・
  リモート画面共有）には映らない
- ペインの角丸（7px）に対し webview は矩形のまま数 px 内側に収まる（見た目の妥協）
