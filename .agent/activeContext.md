# Active Context

> このファイルは AI が**毎ターン上書き更新**する現在状態のスナップショット。
> 過去ログは `progress.md` を見ること。ここには履歴を残さない。
> セッション開始時に AGENTS.md の直後に必ず読む。

## 現在の対象（2026-07-21・#381 + #380 完了）

**複数ウィンドウ是正 2 件が完了**: #381（PR #400 `f9c2ac8`）→ #380（PR #402 `823e149`）。
worker（tako: 381+380）の担当分は終了。

## 次の一手

- **`build-app.sh --install` で修正版を実機反映**（重要: 旧ビルドで「最後のウィンドウ赤ボタン
  close → Dock 復帰」を行うとゾンビ TakoApp が増える。修正版は entity 再利用で構造的に安全）
- ユーザー実機確認: #381（赤ボタン close → Dock 復帰でタブ維持・silent death なし）/
  #380（New Window で全タブ表示・クリックで表示移動・W バッジ）→ 確認後に Issue クローズ（master）
- main のセルフテスト「worker_status IPC（#181）」失敗は #390 worker が対処中（素の main で再現）
- silent death が再発した場合は `<data_dir>/panic.log` を必ず確認（#381 で恒久計装済み）
- v0.6.0 リリース判断（#381 + #380 + 並行 worker 分同梱）

## 現フェーズで Read すべき設計書

- 特になし（直近タスク完了）
