//! tako-app — GPUI バイナリ（UI 層）
//!
//! GPUI への依存はこのクレートだけに閉じ込める（`.agent/architecture.md`）。

fn main() {
    // Phase 1 タスク 3 で PoC 相当の最小ターミナル（1 ペイン）を実装する。
    println!("tako-app: 最小ターミナルは次コミットで実装");
}
